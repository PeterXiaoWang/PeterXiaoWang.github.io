<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[Dart基础语法]]></title>
    <url>%2F2019%2F11%2F26%2FDart%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95%2F</url>
    <content type="text"><![CDATA[Dart语法基础 本文内容主要是对Dart的语法基础进行总结，首先推荐一个线上调试Dart的地址repl.it，下面开始对Dart语法进行介绍。 初体验 试着写一个hello world，你会发现Dart和Java还有Javascript都有些相似。 12345678printInteger(int a) &#123; print(&apos;Hello world, this is $a.&apos;); &#125;main() &#123; var number = 2019; printInteger(number); &#125; Dart的变量与类型 12345var 声明一个变量，表示类型由编译器推断决定Dart 是类型安全语言，all is object，甚至数字、布尔值、函数和 null 也都是继承自 Object 的对象基本类型：num bool String List map 数值类型 Dart 的数值类型 num ，有两种子类 int 和 double 。 12345int x = 1;int hex = 0xEEADBEEF;double y = 1.1;double exponents = 1.13e5;int roundY = y.round(); 布尔值 在 Dart 里，只有两个对象具有 bool 类型：true 和 false，它们都是编译时常量。 Dart 是类型安全的，因此我们不能使用 if(nonbooleanValue) 或 assert(nonbooleanValue) 之类的在 JavaScript 可以正常工作的代码，而应该显式地检查值。 1234// 检查是否为0.var number = 0;assert(number == 0);// assert(number); 错误 string Dart 的 String 由 UTF-16 的字符串组成。和 JavaScript 一样，构造字符串字面量时既能使用单引号也能使用双引号，还能在字符串中嵌入变量或表达式：你可以使用 ${express} 把一个表达式的值放进字符串。而如果是一个标识符，你可以省略{}。 12345var s = &apos;cat&apos;;var s1 = &apos;this is a uppercased string: $&#123;s.toUpperCase()&#125;&apos;;var s2 = &apos;Hello&apos; + &apos; &apos; + &apos;World!&apos; ;var s3 = &quot;&quot;&quot;This is amulti-line string.&quot;&quot;&quot;; List与map 它们的声明和使用很简单，和 JavaScript 中的用法类似。 12345678910var arr1 = [&quot;Tom&quot;, &quot;Andy&quot;, &quot;Jack&quot;];var arr2 = List.of([1,2,3]);arr2.add(499);arr2.forEach((v) =&gt; print(&apos;$&#123;v&#125;&apos;)); var map1 = &#123;&quot;name&quot;: &quot;Tom&quot;, &apos;sex&apos;: &apos;male&apos;&#125;; var map2 = new Map();map2[&apos;name&apos;] = &apos;Tom&apos;;map2[&apos;sex&apos;] = &apos;male&apos;;map2.forEach((k,v) =&gt; print(&apos;$&#123;k&#125;: $&#123;v&#125;&apos;)); 和 Java 语言类似，在初始化集合实例对象时，你可以为它的类型添加约束，也可以用于后续判断集合类型。 下面的这段代码，在增加了类型约束后，语义会更清晰 123456789101112var arr1 = &lt;String&gt;[&apos;Tom&apos;, &apos;Andy&apos;, &apos;Jack&apos;];var arr2 = new List&lt;int&gt;.of([1,2,3]);arr2.add(499);arr2.forEach((v) =&gt; print(&apos;$&#123;v&#125;&apos;));print(arr2 is List&lt;int&gt;); // truevar map1 = &lt;String, String&gt;&#123;&apos;name&apos;: &apos;Tom&apos;,&apos;sex&apos;: &apos;male&apos;,&#125;;var map2 = new Map&lt;String, String&gt;();map2[&apos;name&apos;] = &apos;Tom&apos;;map2[&apos;sex&apos;] = &apos;male&apos;;map2.forEach((k,v) =&gt; print(&apos;$&#123;k&#125;: $&#123;v&#125;&apos;)); print(map2 is Map&lt;String, String&gt;); // true 常量定义 const，表示变量在编译期间即能确定的值； final 则不太一样，用它定义的变量可以在运行时确定值，而一旦确定后就不可再变。 123456final name = &apos;Andy&apos;;const count = 3;var x = 70; var y = 30;final z = x / y; 函数 12345678910111213bool isZero(int number) &#123; //判断整数是否为0 return number == 0; &#125;void printInfo(int number,Function check) &#123; //用check函数来判断整数是否为0 print(&quot;$number is Zero: $&#123;check(number)&#125;&quot;);&#125;Function f = isZero;int x = 10;int y = 0;printInfo(x,f); // 输出 10 is Zero: falseprintInfo(y,f); // 输出 0 is Zero: true 还可以像 JavaScript 语言那样用箭头函数来简化这个函数 123bool isZero(int number) =&gt; number == 0;void printInfo(int number,Function check) =&gt; print(&quot;$number is Zero: $&#123;check(number)&#125;&quot;); C++ 与 Java 提供函数的重载，即提供同名但参数不同的函数。 Dart 认为重载会导致混乱，因此从设计之初就不支持重载，而是提供了可选命名参数和可选参数。 具体方式是，在声明函数时： 给参数增加{}，以 paramName: value 的方式指定调用参数，也就是可选命名参数； 给参数增加 []，则意味着这些参数是可以忽略的，也就是可选参数。 12345678910111213141516171819202122//要达到可选命名参数的用法，那就在定义函数的时候给参数加上 &#123;&#125;void enable1Flags(&#123;bool bold, bool hidden&#125;) =&gt; print(&quot;$bold , $hidden&quot;);//定义可选命名参数时增加默认值void enable2Flags(&#123;bool bold = true, bool hidden = false&#125;) =&gt; print(&quot;$bold ,$hidden&quot;);//可忽略的参数在函数定义时用[]符号指定void enable3Flags(bool bold, [bool hidden]) =&gt; print(&quot;$bold ,$hidden&quot;);//定义可忽略参数时增加默认值void enable4Flags(bool bold, [bool hidden = false]) =&gt; print(&quot;$bold ,$hidden&quot;);//可选命名参数函数调用enable1Flags(bold: true, hidden: false); //true, falseenable1Flags(bold: true); //true, nullenable2Flags(bold: false); //false, false//可忽略参数函数调用enable3Flags(true, false); //true, falseenable3Flags(true,); //true, nullenable4Flags(true); //true, falseenable4Flags(true,true); // true, true 类 Dart 是面向对象的语言，每个对象都是一个类的实例，都继承自顶层类型 Object。 Dart 中并没有 public、protected、private 这些关键字，我们只要在声明变量与方法时，在前面加上“”即可作为 private 方法使用。如果不加“”，则默认为 public。 12345678910111213class Point &#123; num x, y; static num factor = 0; //语法糖，等同于在函数体内：this.x = x;this.y = y; Point(this.x,this.y); void printInfo() =&gt; print(&apos;($x, $y)&apos;); static void printZValue() =&gt; print(&apos;$factor&apos;);&#125;var p = new Point(100,200); // new 关键字可以省略p.printInfo(); // 输出(100, 200);Point.factor = 10;Point.printZValue(); // 输出10 与 C++ 类似，Dart 支持初始化列表。在构造函数的函数体真正执行之前，你还有机会给实例变量赋值，甚至重定向至另一个构造函数。 123456789class Point &#123; num x, y, z; Point(this.x, this.y) : z = 0; // 初始化变量z Point.bottom(num x) : this(x, 0); // 重定向构造函数 void printInfo() =&gt; print(&apos;($x,$y,$z)&apos;);&#125;var p = Point.bottom(100);p.printInfo(); // 输出(100,0,0) 复用 复用的方式一般有两种：继承父类和接口实现。这两方方式的区别如下： 继承父类意味着，子类由父类派生，会自动获取父类的成员变量和方法实现，子类可以根据需要覆写构造函数及父类方法； 接口实现则意味着，子类获取到的仅仅是接口的成员变量符号和方法符号，需要重新实现成员变量，以及方法的声明和初始化，否则编译器会报错。 123456789101112131415161718192021222324252627282930313233class Point &#123; num x = 0, y = 0; void printInfo() =&gt; print(&apos;($x,$y)&apos;);&#125;//Vector继承自Pointclass Vector extends Point&#123; num z = 0; @override void printInfo() =&gt; print(&apos;($x,$y,$z)&apos;); //覆写了printInfo实现&#125;//Coordinate是对Point的接口实现//Dart中，每个类都是接口。Coordinate必须实现Point的除构造函数之外的所有成员函数和成员变量class Coordinate implements Point &#123; num x = 0, y = 0; //成员变量需要重新声明 void printInfo() =&gt; print(&apos;($x,$y)&apos;); //成员函数需要重新声明实现&#125;var xxx = Vector(); xxx ..x = 1 ..y = 2 ..z = 3; //级联运算符，等同于xxx.x=1; xxx.y=2;xxx.z=3;xxx.printInfo(); //输出(1,2,3)var yyy = Coordinate();yyy ..x = 1 ..y = 2; //级联运算符，等同于yyy.x=1; yyy.y=2;yyy.printInfo(); //输出(1,2)print (yyy is Point); //trueprint(yyy is Coordinate); //true 可以看出，子类 Coordinate 采用接口实现的方式，仅仅是获取到了父类 Point 的一个“空壳子”，只能从语义层面当成接口 Point 来用，但并不能复用 Point 的原有实现。那么，我们是否能够找到方法去复用 Point 的对应方法实现呢？ 混入（Mixin） Dart 的继承是单继承，如果上栗中 Coordinate 已经有了父类，又想要去复用 Point 的原有的实现，如何实现？ Dart提供了混入机制，只需要 with 关键字即可。 维基百科：在面向对象的语言中，mixins类是一个可以把自己的方法提供给其他类使用，但却不需要成为其他类的父类。 1234567891011class CoordinateT with Point &#123; CoordinateT() &#123; x = 20; y = 20; &#125;&#125;var yyy = Coordinate();print (yyy is Point); //trueprint(yyy is Coordinate); //trueyyy.printInfo()//(20,20) minxin不是多继承： Mixins is not a way to get multiple inheritance in the classical sense. Mixins is a way to abstract and reuse a family of operations and state. It is similar to the reuse you get from extending a class, but it is compatible with single-inheritance because it is linear. 抽象类 12345678910111213abstract class Animals &#123; num age; void eat() =&gt; print(&quot;eat&quot;); void run();&#125;class Human extends Animals&#123; @override void run() &#123; // TODO: implement run &#125;&#125; 抽象类不能被实例化 1Animals animals= Animals(); //error 总结自：极客时间《Flutter核心技术与实战》-陈航]]></content>
      <categories>
        <category>Flutter</category>
      </categories>
      <tags>
        <tag>Flutter</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[CaseStudy-AsyncTask执行问题小结]]></title>
    <url>%2F2019%2F08%2F23%2FCaseStudy-AsyncTask%E6%89%A7%E8%A1%8C%E9%97%AE%E9%A2%98%E5%B0%8F%E7%BB%93%2F</url>
    <content type="text"></content>
  </entry>
  <entry>
    <title><![CDATA[简单方式实现无限轮播Banner]]></title>
    <url>%2F2018%2F12%2F25%2F%E7%AE%80%E5%8D%95%E6%96%B9%E5%BC%8F%E5%AE%9E%E7%8E%B0%E8%BD%AE%E6%92%AD%E6%8E%A7%E4%BB%B6%2F</url>
    <content type="text"><![CDATA[前言 本文主要介绍一种简单实现无限轮播banner的方式。先来看下最后的效果如下: 思路 实现无限轮播思路很简单：基础控件肯定是选用viewpager，然后结合假数据与viewpager的特殊生命周期方法onPageSelected与onPageScrollStateChanged，最终实现无限轮播效果。 假数据 所谓假数据，就是在原始imageView数组最前与最后各加一个假数据，既在原imageView数组的最前面增加一个与原最后一个imageView相同的imageView，同理在原imageView数组最后增加一个与原始第一个imageView相同的imageView，这样在最后一帧和第一帧进行切换时就会产生一种轮播的假象。结合下图应该就会比较容易理解。 具体实现 有了上文提到的假数据之后，在最后一张图片向右切换时，滑动过程中漏出来的是假的第一张图片，而当这张图片完全展示出来之后，我们需要将整个viewpager定位到index为1的真正的第一张图片。同理，在由第一张图片向左切换时，漏出的是假的最后一张图片，而当这张图片完全展示后，我们需要将整个viewpager定位到index为3的图片。 ps：viewpager的index切换使用了mPager.setCurrentItem(pageIndex, false);方法，这样用户肉眼无法看出来banner进行了切换。 根据上面的思路，现在需要实现一个index的简单计算。这里选择在viewpager的onPageSelected方法来实现index的计算。为什么要选择这个方法呢？我们先来看下官方对这个方法给出的定义： 1234567 /** * This method will be invoked when a new page becomes selected. Animation is not * necessarily complete. * * @param position Position index of the new selected page. */void onPageSelected(int position); 可以看出这个方法是在新的一页被选择后调用，并且会把被选择的position作为参数传入，所以在我们在这个方法上可以轻易的计算出我们希望的index，具体实现代码如下： 1234567891011public void onPageSelected(int i) &#123; pageIndex = i;//i取值从0~4，3个原始数据2个假数据 final int imageViewSize = mImageViews.size();//imageView的总数 if (FAKE_ITEM_COUNT == 2 &amp;&amp; imageViewSize &gt; 1) &#123;//其中FAKE_ITEM_COUNT是指两个假数据 if (i == 0) &#123;// 当视图在第一个时，将页面号设置为图片的最后一张 pageIndex = imageViewSize - FAKE_ITEM_COUNT; &#125; else if (i == mImageViews.size() - 1) &#123;// 当视图在最后一个时,将页面号设置为图片的第一张 pageIndex = 1; &#125; &#125;&#125; 结合代码注释，理解起来应该很容易，这里就不在赘述。现在计算好了index，我们最后只需要在合适的时机进行切换就大功告成了。这个合适的时机就是viewpager滑停的时候，整个实现代码如下： 1234567public void onPageScrollStateChanged(int i) &#123; if (i == ViewPager.SCROLL_STATE_IDLE) &#123; if (mPager.getCurrentItem() != pageIndex) &#123; mPager.setCurrentItem(pageIndex, false); &#125; &#125;&#125; 理解起来也很容易，既当viewpager滑停后，如果viewpager当前的index与当前我们计算的pageIndex不同时就把viewpager切换到pageIndex的位置（index=0，pageIndex=3的场景和index=4，pageIndex=1的场景），这样就实现了无限轮播。 自动播放 实现自动播放的方式是通过handler发一个延迟消息，而在handler的消息处理中只要去设置当前index+1即可 12345int position = mPager.getCurrentItem() + 1;if (position &gt;= mPager.getAdapter().getCount()) &#123; // 滑到底就再滑到开头 position = 0;&#125;mPager.setCurrentItem(position); 最后贴一下整个Demo的地址 https://github.com/PeterXiaoWang/BannerView]]></content>
      <categories>
        <category>安卓笔记</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Flutter for Android走读]]></title>
    <url>%2F2018%2F12%2F22%2FFlutter-for-Android%E8%B5%B0%E8%AF%BB%2F</url>
    <content type="text"><![CDATA[前言 Flutter官方文档有一篇专门为安卓开发者写的Flutter for Android，此篇文章拿安卓相关概念与Flutter进行对比，从而帮助安卓开发者快速了解Flutter。本文目的是对此片文章关键信息进行记录。 Flutter for Android Views Flutter中，Widget代替安卓的view的概念，并且任何东西都Widget。区别在于，Widget仅仅支持一帧，每一帧都会创建Widget实例树，相当于一次性绘制整个界面。Android中的View，在绘制结束之后不会重绘，除非使用invalidate方法。与Android的视图层次系统不同（在framework改变视图），而在Flutter中的widget是不可变的，这允许widget变得超级轻量。 Flutter中的Widget分为有状态的（StatefulWidget）和无状态的（StatelessWidgets），更新一个Widget需要使用新建一个StatefulWidget和一个State对象配合使用。这里要注意的重要一点是无状态和有状态 widget的核心特性是相同的。每一帧它们都会重新构建，不同之处在于StatefulWidget有一个State对象，它可以跨帧存储状态数据并恢复它。 布局方式只能通过布局树实现，不能像安卓样使用xml进行布局。 Widget是不可变的，没有addChild等方法，所以可以通过一个方法动态返回不同的Widget实现布局中的添加或者删除组件的功能。 Flutter中可以通过AnimationController和Animation（例如：CurvedAnimation）实现动画。 可以通过CustomPaint和CustomPainter替代安卓中的Canvas实现在屏幕上绘制自定义形状。 可以通过Widget的组合完成自定义控件（在安卓中是通过继承View）。 Intents Flutter中不存在Intents的概念，与Native的整合中需要用到Intents，另外就是调用外部组件，如Camera或者File picker。Native可以通过MethodChannel与Flutter完成数据通信。 Flutter中，管理多个屏幕有两个核心概念和类：Route 和 Navigator。Route是应用程序的“屏幕”或“页面”的抽象（可以认为是Activity）， Navigator是管理Route的Widget。Navigator可以通过push和pop route以实现页面切换。 处理Flutter中所有路由的Navigator类可用于从已经push到栈的路由中获取结果（实现安卓中的startActivityForResult）。 异步UI Dart是单线程执行模型，除非启动一个Isolate（在另外一个线程上运行Dart代码的方式），否则Dart代码将在UI线程中执行，并由事件循环驱动。 Flutter是单线程的，运行一个事件循环，所以无法进行线程管理或者使用像安卓中AsyncTasks、IntentServices的功能。方法后面增加asyn标识，既可以使此方法异步执行（可以进行耗时操作，如网络请求）。 1234567loadData() async &#123; String dataURL = "https://jsonplaceholder.typicode.com/posts"; http.Response response = await http.get(dataURL); setState(() &#123; widgets = JSON.decode(response.body); &#125;);&#125; 可以通过Isolates执行耗时或计算密集型任务。是一个独立的线程，不能与主线程共享内存。 网络请求可以使用http包。 通过Progress Indicator widget实现进度指示器。 项目结构 Flutter遵循像iOS这样简单的3种分辨率格式: 1x, 2x, and 3x（资源文件放到images中并且需要在pubspec.yaml中进行声明）。 字符串的存储的最好方式是新建一个Strings类，并且在里面存放static类型的string，然后通过Strings类引用字符。 Flutter中通过pubspec.yaml来管理依赖（Android中是gradle）。 Activities和Fragments 在Flutter中Activity和Fragment都对应Widget的概念。 Flutter中可以通过挂接到WidgetsBinding观察并监听didChangeAppLifecycleState更改事件来监听生命周期事件。 12345生命周期事件是：resumed - 应用程序可见并响应用户输入。这是来自Android的onResumeinactive - 应用程序处于非活动状态，并且未接收用户输入。此事件在Android上未使用，仅适用于iOSpaused - 应用程序当前对用户不可见，不响应用户输入，并在后台运行。这是来自Android的暂停suspending - 该应用程序将暂时中止。这在iOS上未使用 Layouts Flutter中可以通过Row和Column实现LinearLayout。 通过使用Column、Row和Stack的组合来实现RelativeLayout的效果。 Flutter中Listview对应安卓中ScrollView、Listview。Flutter实现notifyDataSetChanged需要在setState中创建一个新的List()，并且将所有旧数据复制到新列表中。 ListView.Builder相当于安卓中的RecyclerView。 Text widget上设置自定义字体，需要将字体文件夹放到assets文件夹中，然后在pubspec.yaml中声名。 Text的样式有一个对应TextStyle对象，可以在其中自定义许多参数。 1234567891011121314colordecorationdecorationColordecorationStylefontFamilyfontSizefontStylefontWeighthashCodeheightinheritletterSpacingtextBaselinewordSpacing 可以通过向Text Widget的装饰构造函数参数添加InputDecoration对象，完成输入框显示占位符文本。同样设置InputDecoration的errorText可以显示验证错误。 手势检测和触摸事件处理 Flutter中可以通过Widget的onPressed事件完成点击事件的回调（setOnClickListener）。 如果Widget不支持事件监听，可以将改Widget包装到GestureDetector中，并处理onTap方法。 GestureDetector支持的手势动作。 12345Tap：onTapDown、onTapUp、onTap、onTapCancelDouble tap：onDoubleTap长按：onLongPress垂直拖动：onVerticalDragStart、onVerticalDragUpdate、onVerticalDragEnd水平拖拽：onHorizontalDragStart、onHorizontalDragUpdate、onHorizontalDragEnd 数据库与本地存储 可以通过插件Shared_Preferences来完成对Shared Preferences的操作。 在Flutter中，您可以使用SQFlite插件来访问SQFlite此功能。]]></content>
      <categories>
        <category>Flutter</category>
      </categories>
      <tags>
        <tag>Flutter</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[RecyclerView实现item多类型布局和跨列]]></title>
    <url>%2F2018%2F12%2F21%2FRecyclerView%E5%AE%9E%E7%8E%B0item%E5%A4%9A%E7%B1%BB%E5%9E%8B%E5%B8%83%E5%B1%80%E5%92%8C%E8%B7%A8%E5%88%97%2F</url>
    <content type="text"><![CDATA[背景 本文主要简单记录下RecyclerView如何实现item多类型布局和item跨列。 item多类型布局 首先为RecyclerView设置layoutManger，因为下文要说到跨列，所以这里选用GridLayoutManager。然后为GridLayoutManager设置初始列数，这里设置为两列，代码如下 12manager = new GridLayoutManager(this, 2);recyclerView.setLayoutManager(manager); 接着重写RecyclerView对应adapter的getItemViewType方法，将item的类型标识作为此方法的返回 123public int getItemViewType(int position) &#123; return dataList.get(position).getItemType();&#125; 然后在onCreateViewHolder方法中根据当前viewType的类型去返回对应的viewHolder 1234567891011public ViewHolder onCreateViewHolder(ViewGroup viewGroup, int viewType) &#123; View view = null; if (viewType == ITEM_TYPE_1) &#123; view = View.inflate(mContext, R.layout.item_1, null); return new ViewHolder1(view); &#125; else if (viewType == ITEM_TYPE_2) &#123; view = View.inflate(mContext, R.layout.item_2, null); return new ViewHolder2(view); &#125; return new ViewHolder1(view);&#125; 至此简单的item多类型布局就实现了。 item跨列 item实现跨列主要通过重写GridLayoutManager的setSpanSizeLookup方法来实现，代码如下 1234567891011121314gridLayoutManager.setSpanSizeLookup(new GridLayoutManager.SpanSizeLookup() &#123; @Override public int getSpanSize(int position) &#123; int spanSize; if (adapter.getItemViewType(position) == ITEM_TYPE_1) &#123; spanSize = 2;//跨2列 &#125; else if (adapter.getItemViewType(position) == ITEM_TYPE_2) &#123; spanSize = 1;//跨1列 &#125; else &#123; spanSize = 2; &#125; return spanSize; &#125;&#125;); 此处的spanSize即为需要跨的列数，这里通过当前布局类型来设置对应的spanSize，至此完成跨列。]]></content>
      <categories>
        <category>安卓笔记</category>
      </categories>
      <tags>
        <tag>RecyclerView</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[H5微信支付webview设置referer无效问题]]></title>
    <url>%2F2018%2F12%2F21%2FH5%E5%BE%AE%E4%BF%A1%E6%94%AF%E4%BB%98webview%E8%AE%BE%E7%BD%AEreferer%E6%97%A0%E6%95%88%E9%97%AE%E9%A2%98%2F</url>
    <content type="text"><![CDATA[背景 安卓在接入H5微信支付时，需要在webview中手动设置referer，代码如下 123Map extraHeaders = new HashMap();extraHeaders.put("Referer", "商户申请H5时提交的授权域名");//例如 http://www.baidu.com webView.loadUrl(targetUrl, extraHeaders);//targetUrl为微信下单地址 设置referer的原因是因为微信后端需要验证请求来源，所以如果这个地方没有去设置的话，将无法正常拉起支付，并且webview会跳转如下页面中 但是在测试过程中发现一个兼容性问题：在4.4.4、4.4.3的设备上，下单时一直会跳转到上面的页面中，后来通过抓包发现设置的Referer没有生效。 解决方案 经过研究发现了一个靠谱方案，那就是利用loadDataWithBaseURL方法代替loadUrl方法，通过js去请求微信下单Url，我们先看下loadDataWithBaseURL方法的具体描述如下 1public void loadDataWithBaseURL(String baseUrl, String data, String mimeType, String encoding, String historyUrl) 此方法入参data可以是Html代码片，baseUr为页面的基础Url，用来组织html中的相对路径。 具体做法 我们在data这个参数中传入js代码片去请求微信下单Url，而baseUr设置为授权域名，这样请求微信下单Url时，referer自动会被设置为baseUrl（正常请求的referer默认就是请求的来源地址），最终实现代码如下 1234567891011if (("4.4.3".equals(android.os.Build.VERSION.RELEASE)) || ("4.4.4".equals(android.os.Build.VERSION.RELEASE))) &#123; //兼容这两个版本设置referer无效的问题 view.loadDataWithBaseURL("商户申请H5时提交的授权域名", "&lt;script&gt;window.location.href=\"" + targetUrl + "\";&lt;/script&gt;", "text/html", "utf-8", null);&#125; else &#123; Map&lt;String, String&gt; extraHeaders = new HashMap&lt;&gt;(); extraHeaders.put("Referer", "商户申请H5时提交的授权域名"); view.loadUrl(targetUrl, extraHeaders);&#125; 经过测试，以上代码可以在安卓4.4.X正常拉起H5微信支付，最后贴一下官方总结的微信H5支付常见问题汇总 https://pay.weixin.qq.com/wiki/doc/api/H5.php?chapter=15_4 。希望本文能帮助到遇到相同问题的程序员们！]]></content>
      <categories>
        <category>脱坑指北</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Google支付脱坑指南]]></title>
    <url>%2F2018%2F12%2F20%2FGoogle%E6%94%AF%E4%BB%98%E8%84%B1%E5%9D%91%E6%8C%87%E5%8D%97%2F</url>
    <content type="text"><![CDATA[背景 本文主要介绍使用aidl方式接入google支付时遇到的坑以及注意事项 接入google pay的官方文档：https://developer.android.com/google/play/billing/index.html 官方示例：https://github.com/googlesamples/android-play-billing 结合文档与Demo对内购应该会有一个比较全面的理解。 坑与解 官方Demo中是在客户端中做了支付结果的校验，但推荐支付校验最好是放在服务端中处理。因为支付校验时需要利用专属的app_key，如果放在客户端有一定安全性风险。 在测试google pay的时候需要在developer console后台中创建一个beta或者alpha版本的应用，并且需要上传一个签名的apk，当然前提条件是必须要花25美金申请一个谷歌开发账号。另外需要注意的是此apk必须包含&lt;uses-permission android:name=&quot;com.android.vending.BILLING&quot; /&gt;权限，即内购权限。 当应用处于beta、alpha状态时，只有授权的测试者账号（google 账号）才能在google play中下载此apk。在下图1的地方进行添加测试者操作，添加完成后将2的链接分享给测试者，测试者进行授权操作便可以在google play中下载此apk 可能有的同学会问，那岂不是每次做一些改动是必须重新上传apk然后下载，再进行测试？其实只要保证运行的apk签名与developer console上传的apk签名一致，包名一致，版本号与版本名是上传过的版本号与版本名即可。上传过的版本号和版本名怎么理解呢？ 举个例子如上图，我上传过两个版本，老版本version code=10 、verison name=1.5.3，新版本version code=11 verison name=1.5.3，只要本地运行apk的verison code、version name在上传过的各个版本之中就可以正常测试，如果随便写不存在version code、version name是无法正常调起的支付的。 需要注意的是Android Studio直接运行的签名是默认debug，要保持与 developer console 签名一致可以手动配置debug签名，这样直接通过 Android Studio 运行就可以直接进行支付测试。 可以利用google提供的特有的商品id进行静态响应测试，静态相应测试无需进行支付。 配置商品时需要注意的坑就是要将配置完成的商品状态改为激活状态，不然进行支付时一直提示需要登录。 进行真实测试时，测试的google账号必须绑定有海外购买能力的信用卡或者paypal。 不比担心花销，只要在developer console中配置测试账号，就可以进行真实测试但不会真的扣钱。配置的地方如下图。]]></content>
      <categories>
        <category>脱坑指北</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[LruCache缓存图片研究小结]]></title>
    <url>%2F2018%2F12%2F20%2FLruCache%E7%BC%93%E5%AD%98%E5%9B%BE%E7%89%87%E7%A0%94%E7%A9%B6%E5%B0%8F%E7%BB%93%2F</url>
    <content type="text"><![CDATA[前言 上一篇研究了LinkedHashMap实现LRU策略，虽然通过上述方式来实现图片缓存可以优化内存的使用效率，但是这种方式也存在一些问题，例如，LinkedHashMap不是线程安全的，所以在操作时需要考虑线程安全问题。另外在缓存时，只能指定缓存数据条目的数量，不能指定缓存区的大小，如果需要缓存的图片都比较大，可能就会出现问题。 其实在Android SDK 中已经为我们提供了一个实现LRU策略的cache类，LruCache类，这个类封装了LinkedHashMap并且解决了LinkedHashMap中存在的问题，今天我们以LruCache缓存图片为例研究下这个类。 LruCache 有一点需要提前说明下，今天我们研究的是android.support.v4.util包中的LruCache类而不是android.util包中也有一个LruCache类，这点需要提前说明下，不然很容易蒙圈，因为这两个类中有些方法实现是不同的。 首先我们看一下LruCache的成员变量，代码如下 12345678910private final LinkedHashMap&lt;K, V&gt; map; private int size;//当前容量private int maxSize;//最大容量private int putCount;//put的次数 private int createCount;//create的次数 private int evictionCount;//回收次数private int hitCount;//命中次数private int missCount;//丢失的次数 再看下构造方法。 1234567public LruCache(int maxSize) &#123; if (maxSize &lt;= 0) &#123; throw new IllegalArgumentException("maxSize &lt;= 0"); &#125; this.maxSize = maxSize; this.map = new LinkedHashMap&lt;K, V&gt;(0, 0.75f, true);&#125; 在构造方法中初始化了maxSize和LinkedHashMap，并且LinkedHashMap为按照访问顺序排序（此部分请参照上一篇文章）。maxSize即为最大容量。 前面提到LruCache可以指定缓存区大小，这怎么实现呢？其实，实现起来很简单只要在初始化的时候重写LruCache提供的sizeOf方法即可，代码如下。 12345678910int LRU_CACHE_SIZE = 4 * 1024 * 1024; //4MBLruCache&lt;String,Bitmap&gt; lruCache=new LruCache&lt;String,Bitmap&gt;(LRU_CACHE_SIZE) &#123; @Override protected int sizeOf(String key, Bitmap value) &#123; if (value != null) return value.getByteCount(); else return 0; &#125;&#125;; 首先我们定义了LruCache的最大容量为4 * 1024 * 1024，即4M的空间，然后我们重写sizeOf方法，让这个方法返回Bitmap的字节数。为什么这样写就可以指定缓存区大大小呢？接下来我们看下比较重要的put和get方法，你就会明白了~ 首先看下put方法的源码。 12345678910111213141516171819202122 public final V put(K key, V value) &#123; if (key == null || value == null) &#123; throw new NullPointerException("key == null || value == null"); &#125; V previous; synchronized (this) &#123; putCount++; size += safeSizeOf(key, value); previous = map.put(key, value); if (previous != null) &#123; size -= safeSizeOf(key, previous); &#125; &#125; if (previous != null) &#123; entryRemoved(false, key, previous, value); &#125; trimToSize(maxSize); return previous;&#125; 在方法体中，我们发现当key或者value为空时会抛出异常，这部分和LinkedHashMap是不同的，说明LruCache类是不支持key或者value为空，并且在对map进行put操作时加了synchronized ，保证了线程安全。另外我们发现有如下操作 1size += safeSizeOf(key, value); 这部分看上去像对size（当前容量）的累加，我们继续进入到safeSizeOf方法中 1234567private int safeSizeOf(K key, V value) &#123; int result = sizeOf(key, value); if (result &lt; 0) &#123; throw new IllegalStateException("Negative size: " + key + "=" + value); &#125; return result;&#125; 在这个方法中看到了之前重写sizeOf方法，看到这里我想大家就明白了。**我们之前重写sizeOf方法让它返回bitmap的字节数，而在put方法中会对每次put进来的bimap的字节数利用size进行累加，这样我们利用size和maxSize进行比较，就可以得知当前容量是否大于最大容量，当大于时就可以进行删除最近最少少用的资源的操作了。**另外sizeOf默认情况下是返回1的，所以如果不重写sizeOf方法，LruCache也是进行“计数”的。 接着看put方法的代码，我们会发现如下操作。 1234previous = map.put(key, value);if (previous != null) &#123; size -= safeSizeOf(key, previous);&#125; 我们都知道，在对一个hashmap进行put操作的时候 ，如果put的key-value键值对中的key已经存在与map中，新put的value会覆盖旧value，并且会返回旧value。如果key不存在于map中，则会返回null。这里利用previous获取返回值，如果previous不为空，则当前容量会减去previous的大小，这部分比较好理解，其实就是防止同一个key对应新旧value的大小的重复叠加。 接着往下看，在previous不为空的时候，会调用 entryRemoved(false, key, previous, value) 方法，并且把previous传进去了，查看此方法，我们发现此方法并没有函数体，看来需要我们重写此方法。 1protected void entryRemoved(boolean evicted, K key, V oldValue, V newValue) 根据前面所讲，我们可以在初始化的时候重写这个方法，来释放掉一个key对应的旧value所占用的资源，代码如下 12345678910111213141516LruCache&lt;String,Bitmap&gt; lruCache=new LruCache&lt;String,Bitmap&gt;(LRU_CACHE_SIZE) &#123; @Override protected int sizeOf(String key, Bitmap value) &#123; if (value != null) return value.getByteCount(); else return 0; &#125; @Override protected void entryRemoved(boolean evicted, String key, Bitmap oldValue, Bitmap newValue) &#123; if(evicted==false&amp;&amp;oldValue！=null)&#123; oldValue.recycle();//释放bitmap资源 &#125; &#125;&#125;; 看到这里，可能会有疑问，在什么方法里会进行删除最近最少的操作呢？接着看put方法我们会发现trimToSize方法，答案其实就在这个方法里面。代码如下 12345678910111213141516171819202122232425public void trimToSize(int maxSize) &#123; while (true) &#123;//不断循环删除linkedHashMap首元素，也就是最近最少访问的条目，直到size小于最大容量或者map中已经没有数据 K key; V value; synchronized (this) &#123; if (size &lt; 0 || (map.isEmpty() &amp;&amp; size != 0)) &#123; throw new IllegalStateException(getClass().getName() + ".sizeOf() is reporting inconsistent results!"); &#125; if (size &lt;= maxSize || map.isEmpty()) &#123;//直到当前容量小于最大容量 break; &#125; Map.Entry&lt;K, V&gt; toEvict = map.entrySet().iterator().next();//指向首元素 key = toEvict.getKey(); value = toEvict.getValue(); map.remove(key);//删除最近最少的entry size -= safeSizeOf(key, value); evictionCount++; &#125; entryRemoved(true, key, value, null); &#125;&#125; 观察方法体我们发现，当当前容量大于最大容量时，会不断删除首元素即最近最少访问的元素，然后重新计算当前容量大小。只有在当前容量小于最大容量或者map中没有数据的时候才会break出去。在代码的最后也会调用一次entryRemoved(true, key, value, null)方法，并且把因为空间不足而删除的元素的key和value传递进去，不过第一参数为true有别与先前调用时传递的false，所以根据第一参数区别我们就可以进行一些不一样的操作。 1234567protected void entryRemoved(boolean evicted, String key, Bitmap oldValue, Bitmap newValue) &#123; if(evicted==false&amp;&amp;oldValue!=null)&#123; oldValue.recycle(); &#125;if(evicted==true&amp;&amp;oldValue!=null)&#123; //TODO 根据key value 进行二级缓存 &#125;&#125; 好了，put方法说明的比较详细，相信大家已经有了比较深的了解，接下来get方法我们加快点节奏。get方法源码如下 123456789101112131415161718192021222324252627282930313233343536373839public final V get(K key) &#123; if (key == null) &#123;//key 不允许为空 throw new NullPointerException("key == null"); &#125; V mapValue; synchronized (this) &#123; mapValue = map.get(key); if (mapValue != null) &#123; hitCount++; return mapValue; &#125; missCount++; &#125; V createdValue = create(key);//根据key进行新建 if (createdValue == null) &#123; return null; &#125; synchronized (this) &#123; createCount++; mapValue = map.put(key, createdValue); if (mapValue != null) &#123;//有返回值，说明key对应value已存在，需要进行重现赋值，即取消上一步操作 map.put(key, mapValue); &#125; else &#123; size += safeSizeOf(key, createdValue); &#125; &#125; if (mapValue != null) &#123; entryRemoved(false, key, createdValue, mapValue);//可以释放刚创建的createdValue return mapValue; &#125; else &#123; trimToSize(maxSize); return createdValue;&#125;&#125; 观察代码，我们发现其实lrucache方法中主要是对linkedhashmap进行get操作，这部分不清除的同学可以看下上一篇关于linkedhashmap的研究总结。我们重点研究下通过key获取到的value为空时的case。观察代码，如果如果出现上述情况，会新建一个createdValue,并且把createdValue put到map中，利用mapValue 获取put返回值，继续判断mapValue 是否为空，如果mapValue 不为空说明此时此key在map中存在对应的value,所以接下来需要进行重新赋值，让key对应的value为mapValue 而不是createdValue。如果mapValue为空，说明key对应的value确实为空，需要进行就是重新计算当前size的大小。在代码的最后会判断mapValue是否为空，如果mapValue不为空的时候，会调用entryRemoved方法，并把createdValue放在oldvalue的位置，因为此时createdValue所占的资源是无用的，所以我们可以在entryRemoved中释放createdValue。如果mapValue不为空又会进入到trimToSize方法中进行容量计算和删除“最近最少”。 好了，get也方法讲完了，最后看下remove方法。 12345678910111213141516171819public final V remove(K key) &#123; if (key == null) &#123; throw new NullPointerException("key == null"); &#125; V previous; synchronized (this) &#123; previous = map.remove(key); if (previous != null) &#123; size -= safeSizeOf(key, previous); &#125; &#125; if (previous != null) &#123; entryRemoved(false, key, previous, null); &#125; return previous;&#125; 观察代码，基本上和get和put有异曲同工之妙，在这里就不重复阐述了。 总结 比较重要的方法都讲完了，到此大家对lrucache应该有了一个全面的了解了吧~~接下来我们稍微总结下lrucache吧！ LruCache封装了LinkedHashMap，提供了LRU缓存的功能，并且在关键操作加了synchronized ，实现了线程安全。 LruCache提供了trimToSize方法，当容量不足时会自动删除最近最少访问的键值对。 LruCache提供了entryRemoved(boolean evicted, K key, V oldValue, V newValue)方法,通过重写这个方，结合put,get,remove方法我们可以做更多的事情。 LruCache不允许空键值； LruCache提供了sizeof方法，重写这个方法可以实现指定缓存区大小，而不是像LinkedHashMap一样只能指定缓存条目数。]]></content>
      <categories>
        <category>安卓笔记</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[通过LinkedHashMap缓存图片并实现LRU策略]]></title>
    <url>%2F2018%2F12%2F20%2F%E9%80%9A%E8%BF%87LinkedHashMap%E7%BC%93%E5%AD%98%E5%9B%BE%E7%89%87%E5%B9%B6%E5%AE%9E%E7%8E%B0LRU%E7%AD%96%E7%95%A5%2F</url>
    <content type="text"><![CDATA[前言 在Android开发过程中，实现图片缓存是一个很重要的问题，如果处理不当很容易引起OOM等问题。很多图片加载框架中都会使用LRU机制来优化内存使用率。今天我们就看下通过LinkedHashMap如何实现LRU机制。 LRU（Least Recently Used）策略,即当内存使用不足时，把最近最少使用的数据从缓存中移除，保留使用最频繁的数据。 LinkedHashMap LinkedHashMap，它继承与HashMap，底层使用哈希表与双向链表来保存所有元素，上图即为一个双向链表。其基本操作与父类HashMap相似，它通过重写父类相关的方法，来实现自己的链接列表特性。在初始化一个LinkedHashMap时可以指定accessOrder值来指定链表的排序策略，当accessOrder为false的时候链表按插入顺序排序，默认为false，当为true的时候按访问顺序排序，什么是按访问顺序排序呢？ 12345public LinkedHashMap(int initialCapacity, float loadFactor, boolean accessOrder) &#123; super(initialCapacity, loadFactor); init(); this.accessOrder = accessOrder;&#125; 其实按访问顺序排序就是指在调用get方法后，会将这次访问的元素移至链表尾部，不断访问可以形成按访问顺序排序的链表。这部分源码如下 1234567891011121314151617181920212223public V get(Object key) &#123; if (key == null) &#123; HashMapEntry&lt;K, V&gt; e = entryForNullKey; if (e == null) return null; if (accessOrder) makeTail((LinkedEntry&lt;K, V&gt;) e); return e.value; &#125; int hash = Collections.secondaryHash(key); HashMapEntry&lt;K, V&gt;[] tab = table; for (HashMapEntry&lt;K, V&gt; e = tab[hash &amp; (tab.length - 1)]; e != null; e = e.next) &#123; K eKey = e.key; if (eKey == key || (e.hash == hash &amp;&amp; key.equals(eKey))) &#123; if (accessOrder) makeTail((LinkedEntry&lt;K, V&gt;) e); return e.value; &#125; &#125; return null;&#125; 查看源码我们发现，不管keys是否为空，在accessOrder为true（即按访问顺序排序）后，会先执行makeTail方法，然后再返回元素的Value。我们继续跟到makeTail方法中。 12345678910111213private void makeTail(LinkedEntry&lt;K, V&gt; e) &#123; // Unlink e e.prv.nxt = e.nxt; e.nxt.prv = e.prv; // Relink e as tail LinkedEntry&lt;K, V&gt; header = this.header; LinkedEntry&lt;K, V&gt; oldTail = header.prv; e.nxt = header; e.prv = oldTail; oldTail.nxt = header.prv = e; modCount++;&#125; 观察makeTail的函数体，我们很容易发现这是一个对双向链表中一个元素解绑后与header关联的代码，经过操作后，当前元素会被插入到链表尾部。看到这里，我想大家就明白了为什么通过get方法会使链表按照按访问顺序排序。那当我们put数据的时候又会发生什么呢？通过源码我们发现，当我们put数据的时候，会先执行hashmap的put方法中，这是因为LinkedHashMap并没用重写这个方法而是重写了put方法中所调用的addNewEntry和addNewEntryForNullKey等方法。我们主要看下addNewEntry的函数体。 12345678910111213@Override void addNewEntry(K key, V value, int hash, int index) &#123; LinkedEntry&lt;K, V&gt; header = this.header; LinkedEntry&lt;K, V&gt; eldest = header.nxt; if (eldest != header &amp;&amp; removeEldestEntry(eldest)) &#123; remove(eldest.key); &#125; LinkedEntry&lt;K, V&gt; oldTail = header.prv; LinkedEntry&lt;K, V&gt; newTail = new LinkedEntry&lt;K,V&gt;( key, value, hash, table[index], header, oldTail); table[index] = oldTail.nxt = header.prv = newTail;&#125; addNewEntry方法中首先会获取头指针的下一个元素，并定义为eldest,从字面上理解应该是最老的元素，结合前面get方法的介绍也比较好理解为什么头指针下一个元素会是”最老的“。if中的第一个链表非空的判断比较好理解，然后我们看下removeEldestEntry这个方法 123protected boolean removeEldestEntry(Map.Entry&lt;K, V&gt; eldest) &#123; return false;&#125; 默认倩况下返回false的，那什么时候返回true，只有在removeEldestEntry也返回true的时候才会去执行删除“最老”的元素的操作，这样才比较符合我们的逻辑。其实目前LRU策略少一个判断，就是什么时候去执行删除最近最少操作的数据，重新看下LRU的概念，当内存不足时进行删除，而这个内存对LinkedHashMap来说其实就是大小，所以我们只需要在初始化LinkedHashMap的时候重写这个方法并且做一个判断，如果当前大小已经大于LinkedHashMap的容量就返回true,从而就会删除最近最少的数据，码如下。 1234567891011HashMap&lt;String, Bitmap&gt; hardCache = new LinkedHashMap&lt;String, Bitmap&gt;( 10, 0.75f, true) &#123; @Override protected boolean removeEldestEntry(Entry&lt;String, Bitmap&gt; entry) &#123; if (this.size() &gt; CAPACITY) &#123; return true; &#125; else &#123; return false; &#125; &#125;&#125;; 到这里LinkedHashMap的LRU策略就实现了。下面我们验证下前面所说的。 示例代码： 1234567891011121314151617HashMap&lt;String, String&gt; map=new LinkedHashMap&lt;String,String&gt;(5, 0.75f, true)&#123; protected boolean removeEldestEntry(java.util.Map.Entry&lt;String,String&gt; eldest) &#123; if(this.size()&gt;5)&#123; return true; &#125;else&#123; return false; &#125; &#125;&#125;; for(int i=0;i&lt;5;i++)&#123; map.put("key"+i,"value"+i);&#125; for(String value:map.values())&#123; System.out.println(value);&#125; 输出输出结果为： 12345value0value1value2value3value4 下面我们get下map中的数据 123456789for(int i=0;i&lt;5;i++)&#123; map.put("key"+i,"value"+i);&#125; map.get("key"+3); for(String value:map.values())&#123; System.out.println(value);&#125; 输出结果为： 12345value0value1value2value4value3 结果和我们上面讲的是一致的。接下来改下代码再测试下 12345678910for(int i=0;i&lt;5;i++)&#123; map.put("key"+i,"value"+i);&#125; map.get("key"+3);map.put("key5","value new add"); for(String value:map.values())&#123; System.out.println(value);&#125; 这段代码是向已经满了的LinkedHashMap中继续添加一个元素，根据前所讲，应该会删除第一个元素，即为key0元素被删除，下面看下结果： 12345value1value2value4value3value new add 结果与我们前面所讲的一致。写到这里大家应该对linkedhashmap实现LRU有了一个全面的了解。利用这个机制在缓存图片的时候大大的优化内存使用率，使内存中保留的数据是经常使用的图片。]]></content>
      <categories>
        <category>安卓笔记</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[Hello World]]></title>
    <url>%2F2018%2F12%2F20%2Fhello-world%2F</url>
    <content type="text"><![CDATA[Hello world!]]></content>
  </entry>
</search>
